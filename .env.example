# ============================================
# SupNum RAG Chatbot - Environment Variables
# ============================================

# PostgreSQL Database Configuration
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST=
POSTGRES_PORT=5432

# Qdrant Vector Database Configuration
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=supnum_chunks

# Embedding Model Configuration
# Options:
# - all-MiniLM-L6-v2 (384 dim, FAST, recommended for beginners)
# - all-mpnet-base-v2 (768 dim, better quality but slower)
# - paraphrase-multilingual-MiniLM-L12-v2 (384 dim, good for French)
EMBEDDING_MODEL=all-MiniLM-L6-v2
VECTOR_SIZE=384

# Text Chunking Configuration
CHUNK_SIZE=500
CHUNK_OVERLAP=50

# OpenRouter API Configuration
# GET YOUR KEY FROM: https://openrouter.ai
# Sign up (free) -> Go to "Keys" -> Create Key -> Paste below
OPENROUTER_API_KEY=Your _OpenRouter_API_Key_Here

# LLM Model Selection
# FREE MODELS (no credit card needed):
# - meta-llama/llama-3.1-8b-instruct:free (RECOMMENDED - best free option)
# - mistralai/mistral-7b-instruct:free (fast)
# - google/gemma-2-9b-it:free (good for French)
#
# PAID MODELS (better quality, requires credits):
# - meta-llama/llama-3.1-70b-instruct ($0.50/1M tokens)
# - anthropic/claude-3-haiku ($0.25/1M tokens)
# - openai/gpt-4o-mini ($0.15/1M tokens)
LLM_MODEL=amazon/nova-2-lite-v1:free

# ============================================
# INSTRUCTIONS:
# 1. Replace OPENROUTER_API_KEY= with your actual key
#    Example: OPENROUTER_API_KEY=sk-or-v1-abc123xyz...
#
# 2. Save this file as ".env" in your project root
#
# 3. That's it! You're ready to go.
# ============================================